package agents;

import jade.core.Agent;

public class SupervisorAgent extends Agent {
    protected void setup() {
        // Define behavior for task allocation
        //addBehaviour(new TaskAllocationBehavior());
    }
}
